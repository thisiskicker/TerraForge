apiVersion: v1
kind: Service
metadata:
  name: {{ include "terachart.fullname" . }}
  labels:
    {{- include "terachart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "terachart.selectorLabels" . | nindent 4 }}
